# 🔧 Configuration Environment Variables

# === Cloudflare Configuration ===
# Get your API token from: https://dash.cloudflare.com/profile/api-tokens
# Required permissions: Zone:Zone:Read, Zone:DNS:Edit
CF_API_TOKEN="your_cloudflare_api_token_here"

# Your main domain (Zone ID will be auto-discovered)
DOMAIN_ROOT="ori3com.cloud"

# === Tailscale Configuration ===
# Generate auth key from: https://login.tailscale.com/admin/settings/keys
# Recommend: Reusable key with expiration
TAILSCALE_AUTH_KEY="your_tailscale_auth_key_here"

# === Deployment Configuration ===
# Server suffix: 01, 02, 03, etc.
HOSTNAME_SUFFIX="01"

# Generated automatically (don't edit)
SERVER_NAME="vpn-syncthing-${HOSTNAME_SUFFIX}"
SYNCTHING_HOSTNAME="syncthing-${HOSTNAME_SUFFIX}"

# === Optional: Resource Limits ===
# Docker memory limit (default: 256M)
SYNCTHING_MEMORY_LIMIT="256M"

# Docker CPU limit (default: 0.5)
SYNCTHING_CPU_LIMIT="0.5"
